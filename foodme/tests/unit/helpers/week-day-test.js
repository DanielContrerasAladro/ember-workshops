
import { weekDay } from 'foodme/helpers/week-day';
import { module, test } from 'qunit';

module('Unit | Helper | week day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weekDay([42]);
  assert.ok(result);
});

